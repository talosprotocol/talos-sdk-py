repo_id: "sdks-python"
repo_root: "." 
commands:
  test_entrypoint: "scripts/test.sh"
coverage:
  enabled: false  # TODO: Enable when coverage reports are generated
  format: "cobertura"
  report_path: "artifacts/coverage/coverage.xml"
  thresholds:
    line: 0.80
    branch: 0.60
  path_thresholds:
    "src/talos_sdk/crypto*": { line: 0.80 }
ci:
  default: ["--smoke", "--unit", "--coverage"]
